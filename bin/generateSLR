#!/bin/bash

#Outputs a corrected long read

tmpRes="$1/tmpRes"$$

./bin/SLRgen -k "$2" -l "$1/Alignments/$9" -t "$9" -o "$4" -m "$5" -b "$6" -s "$7" "$3" > "$tmpRes"
cat "$tmpRes" >> "$8"
rm $tmpRes
